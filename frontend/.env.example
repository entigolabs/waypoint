# Copy this file to .env and fill in the values

# Display name shown in the header (default: "Portal")
VITE_APP_NAME=Entigo Portal minimal

# Full URL that EndpointView will fetch automatically on startup
# Leave empty to fetch from the same origin as the frontend
VITE_API_ENDPOINT=
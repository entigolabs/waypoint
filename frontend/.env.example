# Copy this file to .env and fill in the values

# Display name shown in the header (default: "Waypoint")
VITE_APP_NAME=Waypoint

# Full URL that DashboardView will fetch automatically on startup
# Leave empty to fetch from the same origin as the frontend
VITE_API_ENDPOINT=
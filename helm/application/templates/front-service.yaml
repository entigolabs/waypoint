apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-front
  labels:
    app: {{ .Release.Name }}-front
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    app: {{ .Release.Name }}-front
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
